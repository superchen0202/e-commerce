<%= form_with(model: [:admin, category ]) do |form| %>

    <div class="field">
        <%= form.label :name, "分類名稱", class: "label"  %>
        <%= form.text_field :name, class: 'input is-medium' %>
    </div>

    <hr>
    <%= form.submit "新增分類", class: "button is-primary" %>

<% end %>